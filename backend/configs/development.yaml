app:
  name: AUMS
  environment: development
  port: 8080

database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  name: aums_dev
  sslmode: disable

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0

minio:
  endpoint: localhost:9000
  access_key: minioadmin
  secret_key: minioadmin
  use_ssl: false

logging:
  level: debug